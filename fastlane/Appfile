app_identifier("com.zii.flash.accounting")

# Authentication uses an App Store Connect API key (see fastlane/Deliverfile
# and fastlane/asc_api_key.example.json), so no apple_id / team is required here.
